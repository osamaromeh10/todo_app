

import 'package:flutter/material.dart';

class AppIcons{
  static const  Icon logout=Icon(Icons.logout);
  static const  Icon addTask=Icon(Icons.add,color: Colors.white,);
  static const  Icon list=Icon(Icons.list);
  static const  Icon setting=Icon(Icons.settings);
}